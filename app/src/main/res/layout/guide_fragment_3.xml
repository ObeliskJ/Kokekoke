<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="true"
    android:clipToPadding="true"
     >
    <ImageView
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:src="@drawable/guide_fragment_3"
        android:scaleType="fitXY" />
    <View
        android:id="@+id/view"
        android:background="@color/transparent"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        />
    <!--<RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_marginBottom="15dp" >

        <Button
            android:id="@+id/id_btn_not_login"
            android:layout_width="140dp"
            android:layout_height="40dp"
            android:layout_alignParentLeft="true"
            android:layout_marginLeft="20dp"
            android:background="@drawable/guide_fragment_btn_left"
            android:text="随便逛逛"
            android:textColor="#5578FE"
            android:textSize="18sp" />

        <Button
            android:id="@+id/id_btn_do_login"
            android:layout_width="140dp"
            android:layout_height="40dp"
            android:layout_alignParentRight="true"
            android:layout_marginRight="20dp"
            android:background="@drawable/guide_fragment_btn_right"
            android:text="立即登录"
            android:textColor="#ffffff"
            android:textSize="18sp" />
    </RelativeLayout>-->

</RelativeLayout>